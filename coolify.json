{
    "buildCommand": "composer install --no-dev --optimize-autoloader",
    "startCommand": "php-fpm -D && nginx -g 'daemon off;'",
    "variables": {
        "APP_ENV": "production",
        "APP_DEBUG": "false"
    }
}